# N.E.R.D. Migration — Claude Handover (Phase 2)

Welcome. This environment is prepared for the **N.E.R.D. (Ncademi Edtech Research & Data)** tool migration from a Streamlit prototype to a scalable FastAPI/Next.js architecture on GCP.

## Project Context
- **Status**: Phase 1 is complete. The FastAPI wrapper is established, and schemas (Pydantic & Zod) are locked.
- **Current Phase**: **Phase 2: Async Execution**.
- **Source of Truth**: Refer to `MIGRATION_PROMPT.md` for the full technical specification.

## Your Immediate Goal: Phase 2
Implement the asynchronous worker architecture to handle research jobs. The backend must support the two-stage, user-gated research flow (Initial -> Deep-Dive).

### Key Requirements
1. **Async Jobs**: Convert `/research/initial` and `/research/deep-dive` to enqueue jobs.
2. **SSE Status**: Implement the `GET /jobs/{id}` SSE endpoint to stream granular status updates (e.g., `searching_initial`, `validating_links`) and the final result payload.
3. **Core Integrity**: Import and utilize `nerd_core`. **Do not modify the `nerd_core/` package.**
4. **Byte-Fidelity**: Ensure the `ENABLE_AI_INSIGHTS` flag is respected to maintain 1:1 HTML output parity with the legacy system.

## Folder Structure (Scoped for Phase 2)
- `api/`: FastAPI application, endpoints, and shared schemas.
- `nerd_core/`: Core research orchestration and parsing engine (ReadOnly).
- `prompts/`: Jinja2 templates used by `nerd_core`.
- `tests/`: Automated test suite for validation.
- `MIGRATION_PROMPT.md`: Detailed architectural mandates for all phases.

## How to Submit Drafts
1. **Drafting**: Write your code for the Phase 2 implementation.
2. **Delivery**: Upload your files/patches to the **`drafts/`** folder in this Google Drive directory.
3. **Review**: The internal agent will pull your drafts, perform a code review, and execute the E2E test suite before merging into the production branch.

---
*Generated by Gemini CLI Agent*
